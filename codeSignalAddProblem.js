//Write a function that returns the sum of two numbers.

function solution(param1, param2) {
    let a = (-1000<=param1 && -1000 <= param2);
    let b = (1000 >= param1 && 1000>= param2)
    if (a && b){
        let c = param1 + param2;
        return c;
    }
}
